# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=skylake -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
RUSTFLAGS="${RUSTFLAGS} -C target-cpu=skylake"
MAKEOPTS="-j2 -l3"
EMERGE_DEFAULT_OPTS="-avk --jobs=2 --load-average=3"

# NOTE: This stage was built with the bindist USE flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.UTF-8

FEATURES="${FEATURES} getbinpkg binpkg-request-signature parallel-fetch parallel-install"
USE="dbus elogind dist-kernel ugrd sysusers X pulseaudio alsa ffmpeg extra -crypt -epub -webkit -webengine -doc -handbook -man -test -cups -introspection -vala -bluetooth -geolocation -policykit -jack -jack-client -system-pipewire -netifrc -sysvinit -completions -modemmanager -networkmanager -kernel-install -systemd -systemd-boot -dracut -gnome -gnome-keyring -kde -nvenc -ios -ipod"
ACCEPT_LICENSE="-* @BINARY-REDISTRIBUTABLE @EULA"
